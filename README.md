# GUIDE TO RUNNING THIS PROJECT

1. Clone this repository
2. Create virtual env in backned folder. Install requirements.txt modules
3. Get a GROQ api key and to .env in backend folder. Variable name is GROQ_API_KEY
4. Run `npm i` in frontend folder
5. Run logger, server and `npm run dev` in frontend
